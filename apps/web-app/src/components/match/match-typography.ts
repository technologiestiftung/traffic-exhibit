/**
 * Shared body scale for match UI (district, bike-lane pill, “live data” line, etc.).
 * Slightly under default `text-base` / `2xl:text-lg` (15px / 17px). Weight at call sites.
 */
export const MATCH_INLINE_LABEL_SIZE =
	"font-sans text-[15px] 2xl:text-[17px] leading-snug";
